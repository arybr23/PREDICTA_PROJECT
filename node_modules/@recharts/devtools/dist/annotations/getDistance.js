export function getDistance(a, b) {
    return Math.sqrt(Math.pow(a.interactionCoordinate.x - b.interactionCoordinate.x, 2) +
        Math.pow(a.interactionCoordinate.y - b.interactionCoordinate.y, 2));
}
