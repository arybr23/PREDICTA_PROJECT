Your Markdown file is ready
[file-tag: code-generated-file-0-1784559379277628922]

I have built a highly structured, comprehensive layout framework blueprint for all five pages and exported it directly into a production-ready `DESIGN.md` file for your project repository.

Here is a summary of the layout ecosystem documented inside the file:

### 📄 Layout Architecture Mapping inside `DESIGN.md`

1. **Universal Frame Standard:** Maps out the clean layout framework style mimicking your desktop environment—fixed tracking navigation on the left sidebar, operational contextual indicators at the top right, and a scrollable content area in the main viewport canvas.
2. **Dashboard View:** Houses the **36pt–40pt Bold Hero Forecast**, your twin-column layout split (`Required Stocks` side-by-side with `Predicted Item Sales`), and the bottom module highlighting food waste optimization and financial resources retained to emphasize **UN SDG 12**.
3. **Forecast View:** Details the exact placement for the **Feature Influence Chart** (exposing your LightGBM feature weights like `payday_cycle` and `weather_rain`), the confidence progress tracker (94% confidence), and precise target grid metrics (**12pt–13pt**).
4. **Stocks View:** Standardizes an interactive grid listing items by operational status (`🔴 Shortage Risk` vs. `🟢 Safe`) and feeds directly into a calculated checklist widget ($Purchase = Need - Current$) to automate supply chain tracking.
5. **Reports View:** The core loop engine page showing judges the convergence tracking graph (**Predicted vs. Actual Sales**) alongside the critical **Day 7 Calibration Milestone** tag, paired with input modules for nightly data submission.
6. **Profile View:** Handles standard store properties, active database engine diagnostics (such as runtime status and live $MAPE$ error rates), and the structural recipe matrix converting item sales into base ingredient amounts.

The `DESIGN.md` file is structured in plain code-block wireframes, allowing to use it as an immediate template guide while styling elements or coding the views. Ready to tackle the logic behind the **Data Entry Submission** loop next, or focus on detailing the component animations for the judges?
